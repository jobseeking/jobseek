6. chmod -R 777 storage (skip?)
7. sudo chown -R www-data:www-data jobseeking/


1. "composer install"    (download 3rd party lib to /vendor)
2. "composer update"     (download 3rd party lib to /vendor)
3. config .env (DB connection)
4. "php artisan migrate"   (create DB table)
5  config baseURL.js


