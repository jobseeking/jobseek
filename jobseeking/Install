1. "git clone https://github.com/jobseeking/jobseek.git"

2. "sudo chown -R ubuntu:www-data jobseek"

3. go to project root
  "composer install"    (download 3rd party lib to /vendor)
   
4. config .env (DB connection)

5. "php artisan migrate"   (create DB table)

6  config baseURL : (backend base Controller)



 
No need : 
"chmod -R 777 storage"
"composer update"     (download 3rd party lib to /vendor)
config frontend baseURL.js

----------------------------------------------------------------

[How to clone project to other path]
1. cp -a src dest
2. edit base url in base Controller  Ex : $base_url = "/test_jobseek/public";
3. "sudo chown -R ubuntu:www-data test_jobseek"

----------------------------------------------------------------

